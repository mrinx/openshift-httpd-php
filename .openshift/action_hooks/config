#!/bin/bash

PHP_VERSION='5.6.0'
LIBTOOL_VERSION='2.4.2'
LIBMCRYPT_VERSION='2.5.8'
HTTPD_VERSION='2.4.10'
APR_VERSION='1.5.1'
APR_UTIL_VERSION='1.5.4'
PCRE_VERSION='8.35'

ROOT_DIR=${OPENSHIFT_DIY_DIR%/}/mine
BUILD_DIR=${OPENSHIFT_TMP_DIR%/}/build
AH_DIR=${OPENSHIFT_REPO_DIR%/}/.openshift/action_hooks
WWW_DIR=${OPENSHIFT_REPO_DIR%/}/www

mkdir -p ${ROOT_DIR}
mkdir -p ${WWW_DIR}
